import subprocess
# subprocess.call(["ls", "-l", "/etc/resolv.conf"])
# subprocess.call(["ls"])
import os
print(os.getcwd())
# '/home/user'
# os.chdir("/tmp/")
# subprocess.call(["ls"])

# os.getcwd()
# '/tmp'